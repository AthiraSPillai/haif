{
  "name": "haif-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Human-Agent Intent Framework workspace",
  "type": "module",
  "scripts": {
    "build": "npm run build -w @haif/cli",
    "test": "npm run test -w @haif/cli",
    "haif": "node packages/cli/dist/index.js"
  },
  "workspaces": [
    "packages/cli"
  ],
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
